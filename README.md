# big-brother-media
This project uses watches a mounted dir and uses AI to auto-classify the files landing in it. After classification the files are hard linked to their corresponding media type dirs.
